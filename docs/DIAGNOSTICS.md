# Runtime diagnostics

> Generated by `tools/gen_diagnostics_index.py`. Do not edit by hand.

Every environment variable the runtime reads, with where it is read and
whatever the source says about it. **553 knobs across 86 groups.**

Most are read once, at first use, and cached — so they are set before launch,
not changed while a title runs.

| group | knobs |
|---|---|
| `SPU_*` | 86 |
| `PS3_*` | 67 |
| `YDKJ_*` | 54 |
| `RSX_*` | 49 |
| `PPU_*` | 34 |
| `SPURS_*` | 23 |
| `GCM_*` | 21 |
| `LD_*` | 19 |
| `FP_*` | 14 |
| `TEX_*` | 14 |
| `VP_*` | 13 |
| `PS1_*` | 11 |
| `FLOW_*` | 10 |
| `LBP_*` | 10 |
| `CELLMARK_*` | 8 |
| `PAD_*` | 7 |
| `RTT_*` | 7 |
| `FS_*` | 6 |
| `RT_*` | 6 |
| `DRAW_*` | 6 |
| `RD_*` | 4 |
| `TCONST_*` | 4 |
| `SEM_*` | 3 |
| `SYS_*` | 3 |
| `DBG_*` | 3 |
| `VTX_*` | 3 |
| `TTY_*` | 2 |
| `GCM2D_*` | 2 |
| `A2O_*` | 2 |
| `SUBVP_*` | 2 |
| `D3D12_*` | 2 |
| `CULL_*` | 2 |
| `MOVIE_*` | 2 |
| `VRAMSCAN_*` | 2 |
| `HLE_*` | 1 |
| `RESV_*` | 1 |
| `PT_*` | 1 |
| `ALLOCPROBE_*` | 1 |
| `BCTRL_*` | 1 |
| `LWM_*` | 1 |
| `WATCHDOG_*` | 1 |
| `LV2_*` | 1 |
| `WAITBT_*` | 1 |
| `POKESEM_*` | 1 |
| `POKE7_*` | 1 |
| `SEMTID_*` | 1 |
| `SEMCHAIN_*` | 1 |
| `POLLSITE_*` | 1 |
| `AUDIO_*` | 1 |
| `ADEC_*` | 1 |
| `SYNC_*` | 1 |
| `MSGDIALOG_*` | 1 |
| `FLIP_*` | 1 |
| `NV309E_*` | 1 |
| `NV3089_*` | 1 |
| `SURFDBG_*` | 1 |
| `VDATA_*` | 1 |
| `LOAD_*` | 1 |
| `SEQ_*` | 1 |
| `MVPDBG_*` | 1 |
| `PERF_*` | 1 |
| `TEXUP_*` | 1 |
| `BLENDDBG_*` | 1 |
| `HASHDBG_*` | 1 |
| `PSOMISSDBG_*` | 1 |
| `CUBEKEYDBG_*` | 1 |
| `CMASK_*` | 1 |
| `DEPTH_*` | 1 |
| `REMAPDBG_*` | 1 |
| `CUBEDBG_*` | 1 |
| `NO_*` | 1 |
| `FPK_*` | 1 |
| `SCREEN_*` | 1 |
| `SCREENCOPY_*` | 1 |
| `EMPTYTEX_*` | 1 |
| `SCREENTEX_*` | 1 |
| `DRAWARGS_*` | 1 |
| `MEMPEEK_*` | 1 |
| `FRAME_*` | 1 |
| `CLEAR_*` | 1 |
| `VBFULL_*` | 1 |
| `IDXDBG_*` | 1 |
| `MVP_*` | 1 |
| `IDX_*` | 1 |
| `TEXFMTDBG_*` | 1 |
| `PS3RECOMP_*` | 1 |

## `A2O`

- **`A2O_DBG`** — _(no description in source)_
  `libs/video/cellGcmSys.c:2087`
- **`A2O_FAILDBG`** — _(no description in source)_
  `libs/video/cellGcmSys.c:2138`

## `ADEC`

- **`ADEC_WHO`** — _(no description in source)_
  `libs/codec/cellAdec.c:185`

## `ALLOCPROBE`

- **`ALLOCPROBE`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1491`  _(+1 more sites)_

## `AUDIO`

- **`AUDIO_PEAK`** — AUDIO_PEAK diag: which blocks of this port's WHOLE ring hold data, vs which block the read cursor is on -- distinguishes "writer never writes" from "writer and reader cycle out of phase". */
  `libs/audio/cellAudio.c:416`  _(+1 more sites)_

## `BCTRL`

- **`BCTRL_RING`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1579`  _(+1 more sites)_

## `BLENDDBG`

- **`BLENDDBG`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:1893`  _(+1 more sites)_

## `CELLMARK`

- **`CELLMARK_BLINKDBG`** — _(no description in source)_
  `libs/video/cellGcmSys.c:1772`  _(+1 more sites)_
- **`CELLMARK_DUMP`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:6582`
- **`CELLMARK_DUMP_DIR`** — default: current dir
  `libs/video/rsx_d3d12_backend.c:1296`
- **`CELLMARK_DUMP_EVERY`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:4863`
- **`CELLMARK_DUMP_MINDRAWS`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:4843`  _(+1 more sites)_
- **`CELLMARK_DUMP_SKIP`** — CELLMARK_DUMP_SKIP=N: ignore the first N presents before dumping. The opening frames are the loading screen; the interesting content only appears once the sim SPUs have advanced the scene. */
  `libs/video/rsx_d3d12_backend.c:6588`
- **`CELLMARK_HOLD`** — The RSX present thread runs at ~60Hz; a title that finishes in a few ms would tear down before the first frame is ever presented. Hold indefinitely with CELLMARK_HOLD so the last rendered frame is visible / capturable. */
  `libs/system/sysPrxForUser.c:166`
- **`CELLMARK_IGPU`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:582`

## `CLEAR`

- **`CLEAR_RGB`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:5178`

## `CMASK`

- **`CMASK_FORCE`** — therefore means the guest EXPLICITLY masked every channel -- a depth-only pass (e.g. DeferredShading's shadow-map generation). Honour it: forcing 0 back to 0xF splatters the depth pass's fragment colour onto the target. */ CMASK_FORCE=1: ignore the guest colour mask (write RGBA on every draw). A mis-decoded mask makes geometry rasterize correctly and write nothing, which is indistinguishable from "the draw never happened". */
  `libs/video/rsx_d3d12_backend.c:2329`

## `CUBEDBG`

- **`CUBEDBG`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:2689`  _(+1 more sites)_

## `CUBEKEYDBG`

- **`CUBEKEYDBG`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:2054`

## `CULL`

- **`CULL_FLIP`** — CULL_FLIP=1: invert the front-face convention. Our VP path may hand D3D the opposite winding from the guest's, in which case honouring CULL_FACE culls exactly the faces it should keep. */
  `libs/video/rsx_d3d12_backend.c:2298`
- **`CULL_OFF`** — Guest face culling. CULL_OFF=1 forces double-sided, the old unconditional behaviour -- geometry that vanishes under culling is a winding/front-face problem, not a missing draw. */
  `libs/video/rsx_d3d12_backend.c:2291`

## `D3D12`

- **`D3D12_DBG`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:549`
- **`D3D12_IQ`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:4307`  _(+1 more sites)_

## `DBG`

- **`DBG_CENTER`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:3167`
- **`DBG_LOCK`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:3172`
- **`DBG_ZOOM`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:3164`

## `DEPTH`

- **`DEPTH_OFF`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:2344`

## `DRAW`

- **`DRAW_KEEP_BLEND`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:3979`
- **`DRAW_KEEP_NOCLEAR`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:3973`
- **`DRAW_KEEP_TEX`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:3965`
- **`DRAW_LAST_TEX`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:4007`
- **`DRAW_LIMIT`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:3942`
- **`DRAW_SKIP_TEX`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:3949`

## `DRAWARGS`

- **`DRAWARGS`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:4601`

## `EMPTYTEX`

- **`EMPTYTEX`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:4067`

## `FLIP`

- **`FLIP_DBG`** — _(no description in source)_
  `libs/video/cellGcmSys.c:540`  _(+2 more sites)_

## `FLOW`

- **`FLOW_CONDKICK`** — FLOW_CONDKICK (SPU-bring-up diagnostic): cond=7 is waited on but never signaled (its signaler is blocked on the dead SPU pipeline). Cap infinite waits and return CELL_OK so the engine can advance past spurious waits. */
  `runtime/syscalls/sys_cond.c:252`
- **`FLOW_FORCE_RTCFG`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1686`
- **`FLOW_FSDBG`** — _(no description in source)_
  `runtime/syscalls/sys_fs.c:473`
- **`FLOW_LOCKOK`** — _(no description in source)_
  `libs/system/sysPrxForUser.c:488`
- **`FLOW_LWMTRACE`** — _(no description in source)_
  `libs/system/sysPrxForUser.c:389`
- **`FLOW_NOSPILL`** — import call leaves the caller with a garbage r2 -> all later TOC-relative loads (the C++ ctor list, globals, ...) read garbage -> boot corruption. */ FLOW_NOSPILL: with the lifted code using a constant main TOC (--main-toc/TOCFIX), the caller no longer reads [r1+0x28] to restore r2, so this ABI TOC-spill is unnecessary — and in the frameless-cascade it can clobber a caller frame slot (e.g. the deserializer's this-pointer). Skip it to test that theory. */
  `runtime/ppu/ppu_hle.cpp:294`
- **`FLOW_PSSGTRACE`** — DIAGNOSTIC (FLOW_PSSGTRACE=1): when the title's tty output carries a PhyreEngine init-failure fragment, dump the HOST backtrace (RVAs vs the exe base) so we can map the error-reporter's call chain through build_boot/flow.map and locate the failing function. */
  `runtime/ppu/ppu_loader.cpp:2314`  _(+1 more sites)_
- **`FLOW_SPURSKERNEL`** — _(no description in source)_
  `runtime/ppu/ppu_hle.cpp:588`
- **`FLOW_TITLEFIX`** — DIAGNOSTIC (FLOW_TITLEFIX): the game builds the title path with an EMPTY filename (a stale-lift string-construction bug) -> opens the dir. Redirect a bare ".../Data/Titles/" open to the language file so we can confirm the render path. */
  `runtime/syscalls/sys_fs.c:287`
- **`FLOW_TITLEOPEN`** — _(no description in source)_
  `runtime/syscalls/sys_fs.c:273`

## `FP`

- **`FP_CONSTBUF`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:1995`  _(+1 more sites)_
- **`FP_DUMP`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:2074`
- **`FP_FORCE`** — FP_FORCE=1: replace the translated body with solid magenta -- isolates geometry/transform problems from texture/blend problems. */
  `libs/video/rsx_d3d12_backend.c:2256`
- **`FP_IDCOLOR`** — FP_IDCOLOR=1: give every fragment program a distinct flat colour derived from its address. One frame then shows which program paints which surface, instead of one run per candidate to test them by elimination. */
  `libs/video/rsx_d3d12_backend.c:2188`
- **`FP_KDBG`** — _(no description in source)_
  `libs/video/rsx_fp_decompiler.c:966`
- **`FP_KILL`** — at directly. Restrict with FP_TEX_FP=<hex>. Reading a 69-instruction shader to guess which term carries a bad value is slower and less certain than displaying the terms. */ FP_KILL=<hex fp_addr>: make that program discard every fragment, so what it covers becomes visible. Distinguishes "this surface shades black" from "this surface is correct and something else is missing behind it". */
  `libs/video/rsx_d3d12_backend.c:2133`
- **`FP_LIST`** — FP_LIST=1: every program the title actually compiles, with its size. A fragment program that hangs the GPU shows up here as an implausible instruction count long before it shows up as a TDR. */
  `libs/video/rsx_d3d12_backend.c:2071`
- **`FP_OFF`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:1970`
- **`FP_ONE`** — Debug FP_ONE=<hex fp_addr>: force that program's colour output to all-ones (e.g. wave's colour-detect -> full mask -> island borders everywhere -> the water sim must visibly radiate if it works). */
  `libs/video/rsx_d3d12_backend.c:2099`
- **`FP_PICK`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:2086`
- **`FP_SHOW`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:2146`
- **`FP_TEX`** — FP_TEX=<unit>: output that unit's sample verbatim. FP_TEX_FP=<hex> limits it to one program, so one surface can be inspected without repainting the whole scene. FP_TEX_UV=1 samples with the same texcoord the program uses for that unit rather than tc0. */
  `libs/video/rsx_d3d12_backend.c:2222`  _(+1 more sites)_
- **`FP_TEX_FP`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:2147`  _(+2 more sites)_
- **`FP_TEX_UV`** — FP_TEX_UV=1: sample at a fixed centre UV instead of the interpolated texcoord. If the texture is populated but the texcoords never arrive, the normal path samples texel (0,0) -- usually black -- and looks exactly like an empty texture. This tells the two apart. */
  `libs/video/rsx_d3d12_backend.c:2236`

## `FPK`

- **`FPK_DBG`** — FPK_DBG=1: the fragment constants as the shader will see them. A program whose only output is fp_k[0].xxxx renders black if those constants never arrive, which looks identical from the outside to geometry that never rasterised. */
  `libs/video/rsx_d3d12_backend.c:3087`

## `FRAME`

- **`FRAME_BUDGET`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:4707`

## `FS`

- **`FS_CALLER`** — _(no description in source)_
  `runtime/ppu/ppu_fs.cpp:234`  _(+3 more sites)_
- **`FS_FSTAT_CAP`** — FS_FSTAT_CAP=<bytes>: DIAGNOSTIC. Report a smaller size than the file has. You Don't Know Jack reads a 100 KB archive whole but only probes (open/fstat/close) its 500 KB and 2.4 MB ones; if that branch is driven by the size it just asked for, capping it makes the title take the read path. Answers whether the split is size-based -- nothing more. */
  `runtime/ppu/ppu_fs.cpp:538`
- **`FS_FSTAT_CAP_PATH`** — _(no description in source)_
  `runtime/ppu/ppu_fs.cpp:539`
- **`FS_READ_ALL`** — _(no description in source)_
  `runtime/ppu/ppu_fs.cpp:412`
- **`FS_READ_PATH`** — _(no description in source)_
  `runtime/ppu/ppu_fs.cpp:416`
- **`FS_STACK`** — FS_STACK=<substr>: every open funnels through one guest wrapper, so one caller level says nothing about WHICH subsystem wanted the file. Dump the guest call chain for the opens that matter. */
  `runtime/ppu/ppu_fs.cpp:239`

## `GCM`

- **`GCM_CTXDBG`** — GCM_CTXDBG=1: which context the title is actually driving. Ours is written once at init; if the title repoints gCellGcmCurrentContext at a buffer of its own, every field below moves with it and the FIFO the runtime walks stops being the one the title fills. Reported on change, not per tick. */
  `libs/video/cellGcmSys.c:1270`
- **`GCM_DEFAULT_CTX_REPOINT`** — GCM_DEFAULT_CTX_REPOINT=1 does the real thing -- write our context's EA into the guest variable cellGcmInit's ctx_out pointed at, and rewind that context. Off by default because a title managing its own segment chain may hold pointers into the old one; this exists to be measured, not assumed. */
  `libs/video/cellGcmSys.c:2750`
- **`GCM_DRAINDBG`** — _(no description in source)_
  `libs/video/cellGcmSys.c:1286`  _(+1 more sites)_
- **`GCM_FIFO_CIRCLE_PASSES`** — _(no description in source)_
  `libs/video/cellGcmSys.c:1631`
- **`GCM_FIFO_SNAP`** — _(no description in source)_
  `libs/video/cellGcmSys.c:1321`
- **`GCM_FLIPCB_ONTICK`** — _(no description in source)_
  `libs/video/cellGcmSys.c:162`
- **`GCM_FLIPCOUNT`** — _(no description in source)_
  `libs/video/cellGcmSys.c:1388`  _(+1 more sites)_
- **`GCM_FLIP_BT`** — _(no description in source)_
  `libs/video/cellGcmSys.c:1889`
- **`GCM_FLIP_NEEDS_FIFO`** — _(no description in source)_
  `libs/video/cellGcmSys.c:608`
- **`GCM_GET_EQ_PUT`** — _(no description in source)_
  `libs/video/cellGcmSys.c:1739`
- **`GCM_GUEST_FB`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:3692`  _(+1 more sites)_
- **`GCM_OBJDBG`** — _(no description in source)_
  `libs/video/cellGcmSys.c:1539`
- **`GCM_RATE`** — _(no description in source)_
  `libs/video/cellGcmSys.c:1236`
- **`GCM_RECDBG`** — _(no description in source)_
  `libs/video/cellGcmSys.c:1101`  _(+3 more sites)_
- **`GCM_REFLOG`** — _(no description in source)_
  `libs/video/cellGcmSys.c:1121`
- **`GCM_REFPOLL`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:992`
- **`GCM_REFPUB`** — stderr, deliberately: [DRAIN] uses printf and stdout is block-buffered when redirected, so its interleaving with an stderr probe is not a timeline. Same stream = same ordering. */
  `libs/video/cellGcmSys.c:1156`
- **`GCM_SCANREF`** — _(no description in source)_
  `libs/video/cellGcmSys.c:1299`
- **`GCM_SEMA_ACQUIRE`** — _(no description in source)_
  `libs/video/cellGcmSys.c:1500`
- **`GCM_SUBCH1_2D`** — _(no description in source)_
  `libs/video/cellGcmSys.c:1563`
- **`GCM_SUBCH7`** — _(no description in source)_
  `libs/video/cellGcmSys.c:1519`

## `GCM2D`

- **`GCM2D_DBG`** — _(no description in source)_
  `libs/video/cellGcmSys.c:993`
- **`GCM2D_TRACE`** — _(no description in source)_
  `libs/video/cellGcmSys.c:892`

## `HASHDBG`

- **`HASHDBG`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:2023`

## `HLE`

- **`HLE_BT_EVERY`** — _(no description in source)_
  `runtime/ppu/ppu_hle.cpp:178`

## `IDX`

- **`IDX_DBG`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:5933`

## `IDXDBG`

- **`IDXDBG`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:5607`  _(+1 more sites)_

## `LBP`

- **`LBP_HLE_JOBDONE`** — _(no description in source)_
  `runtime/spu/spu_channels.c:1913`
- **`LBP_NO_TASKSET`** — null base and DMAs EVERYTHING from EA 0 (observed on LBP's FMOD mixer: 105k GETs from ea=0x0, degenerate handler targets -> branch-to-0, and the PPU pump blocks forever on EventFlag 0x0100). Planting it makes the mixer read the real FMOD control block (0x0094F5xx) and the EventFlag handshake completes. Default ON; LBP_NO_TASKSET restores the old direct-dispatch for comparison. */
  `runtime/spu/spu_workload.c:543`
- **`LBP_PERSIST_LS`** — _(no description in source)_
  `runtime/spu/spu_workload.c:296`
- **`LBP_POLICY_CTX`** — EMPIRICAL TEST (LBP_POLICY_CTX): overlay the REAL policy's SpursTasksetContext onto binkspu's LS 0x2700, replacing the C reimpl, then let binkspu run with it. If the movie plane fills (not green) the policy's fuller ctx is the fix (H1); if still green, the frame-output gate is Bink-layer (H2). Keeps the build_context 0x100 kernel ctx (moduleId "TK") intact. */
  `runtime/spu/spu_workload.c:629`
- **`LBP_REAL_POLICY`** — _(no description in source)_
  `runtime/spu/spu_workload.c:597`
- **`LBP_SKIP_NULL_DMA`** — _(no description in source)_
  `runtime/spu/spu_dma.h:521`
- **`LBP_SPU_SERIAL`** — _(no description in source)_
  `runtime/spu/spu_workload.c:309`
- **`LBP_SYNC_ACK`** — ticket u16 @+0, consumer lanes @+2..) as tasks are processed; our HLE runs the task on a dedicated thread and re-runs the PM ~never, so the lanes froze and the skip path's min-of-lanes barrier (sub_4D3730) hung forever. A task ENTERING WAIT_SIGNAL has by definition drained all work dispatched to it, so advancing the active lanes to the published ticket here is semantically the PM's bookkeeping, batched. Validation gate. */
  `runtime/spu/spu_workload.c:950`
- **`LBP_TASK_DELAY`** — _(no description in source)_
  `runtime/spu/spu_workload.c:526`
- **`LBP_WS_DRAIN`** — one-shot SPU model the producer task ran to completion already, so the signal never arrives and the consumer blocks forever (movie/audio/loading deadlock). If set, after <secs> of no real signal we RETURN rc=0 as if the kernel resumed the task with "no pending work" -- lets it run its post-wait path (complete its loop / set completion flags) instead of hanging. 0/unset keeps the block-forever behaviour. Reversible, opt-in. */
  `runtime/spu/spu_workload.c:980`

## `LD`

- **`LD_ALIAS_DBG`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:6430`
- **`LD_CLEAR_TEST`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:6819`
- **`LD_FBDBG`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:2423`
- **`LD_FORCE_A1_OPAQUE`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:1803`
- **`LD_FORCE_GREEN`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:4101`
- **`LD_FRAME_DUMP`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:8046`
- **`LD_FRAME_DUMP_EVERY`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:8047`
- **`LD_HLSL_DUMP`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:4118`
- **`LD_PLANE_DUMP`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:2227`
- **`LD_PRESENT_DBG`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:8100`
- **`LD_PS1_BUF0`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:6321`
- **`LD_PSO_DBG`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:3820`  _(+1 more sites)_
- **`LD_SURF_DUMP`** — LD_FRAME_DUMP only reads back the presented surface, which answers "is the screen black" (it is: nonblack=0 on every frame) but not WHERE the pixels stop. ps1_netemu composites PS1 VRAM into a 720x512 surface and then upscales into the presented 1280x720 one, so the interesting question is which of those is already empty. */
  `libs/video/rsx_live_draw.c:8012`
- **`LD_SURF_DUMP_FRAME`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:8013`
- **`LD_SURF_DUMP_ON_VRAM`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:8019`
- **`LD_TEXRGBA_DUMP`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:2260`
- **`LD_TEX_RACE`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:2398`
- **`LD_UNITS`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:6281`
- **`LD_UVDBG`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:6364`

## `LOAD`

- **`LOAD_DBG`** — _(no description in source)_
  `libs/video/rsx_commands.c:578`

## `LV2`

- **`LV2_ERRDBG`** — LV2_ERRDBG=1: every syscall that returns non-OK, deduped by (number, result). A guest that asserts on a result once per frame is easier to find from this side than by reading its lifted code. */
  `runtime/syscalls/lv2_register.c:2070`

## `LWM`

- **`LWM_COUNT`** — _(no description in source)_
  `runtime/ppu/ppu_sysprx.cpp:118`  _(+1 more sites)_

## `MEMPEEK`

- **`MEMPEEK`** — MEMPEEK=<hex addr>:<n>: n big-endian floats at a guest address each frame, with a min/max so a buffer of constants is distinguishable from real data. */
  `libs/video/rsx_d3d12_backend.c:4662`

## `MOVIE`

- **`MOVIE_BIND`** — MOVIE_BIND=1: trace movie-plane binds (640x360 Y / 320x180 U/V) with the resolved EA + a content probe -- used to diagnose the Bink frame-buffer ring mismatch (the draw binds a cleared buffer 0x4D80 before the one the decoder actually fills). */
  `libs/video/rsx_d3d12_backend.c:6373`
- **`MOVIE_FIND`** — MOVIE_FIND=1: when a 640x360 movie plane uploads ZERO, scan a wide guest window for the REAL frame (a 640-wide region with content) so we learn where the video decode actually wrote vs where the texture points -- ends the frame-buffer-address guessing. */
  `libs/video/rsx_d3d12_backend.c:2707`

## `MSGDIALOG`

- **`MSGDIALOG_ANSWER`** — _(no description in source)_
  `libs/system/cellMsgDialog.c:119`

## `MVP`

- **`MVP_BASE`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:5773`  _(+1 more sites)_

## `MVPDBG`

- **`MVPDBG`** — MVPDBG: dump every non-zero vertex-constant slot the FIRST time a G-buffer draw (surf0=0xCC0000) is dispatched -- the true MVP the GPU will use, read live (no snapshot/parity indirection). */
  `libs/video/rsx_commands.c:683`

## `NO`

- **`NO_ALPHATEST`** — rsx_alphatest (b1[4]): x=enable, y=ref/255, z=func-0x200. D3D12 has no fixed alpha test; guest FPs discard on it dynamically (wave's colour wheel is a disc via alpha test -- without it the palette drew as an opaque square). */
  `libs/video/rsx_d3d12_backend.c:3058`

## `NV3089`

- **`NV3089_DBG`** — _(no description in source)_
  `libs/video/cellGcmSys.c:830`  _(+1 more sites)_

## `NV309E`

- **`NV309E_BLIT`** — On by default: this is the mip/blur pyramid. Without it textures have no mip levels, which shows up as aliased blocky tiles and a noise patch where a minified surface should be, and the shell that samples the pyramid draws untextured. NV309E_BLIT=0 disables. */
  `libs/video/cellGcmSys.c:807`

## `PAD`

- **`PAD_ALWAYS_CHANGE`** — _(no description in source)_
  `libs/input/cellPad.c:789`
- **`PAD_AUTOPRESS`** — _(no description in source)_
  `libs/input/cellPad.c:595`
- **`PAD_FILE`** — _(no description in source)_
  `libs/input/cellPad.c:665`
- **`PAD_NO_KEYBOARD`** — _(no description in source)_
  `libs/input/cellPad.c:146`
- **`PAD_SCRIPT`** — _(no description in source)_
  `libs/input/cellPad.c:617`
- **`PAD_STICK`** — _(no description in source)_
  `libs/input/cellPad.c:695`
- **`PAD_SWEEP`** — _(no description in source)_
  `libs/input/cellPad.c:710`

## `PERF`

- **`PERF`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:474`

## `POKE7`

- **`POKE7`** — POKE7=<id>: one-shot force-post of an arbitrary semaphore, to test whether a starved gate (e.g. the completion sem tid0 polls) is merely a lost wakeup -- if the load then proceeds, the state was ready and only the signal was lost. */
  `runtime/syscalls/sys_semaphore.c:124`

## `POKESEM`

- **`POKESEM`** — _(no description in source)_
  `runtime/syscalls/sys_semaphore.c:86`

## `POLLSITE`

- **`POLLSITE`** — POLLSITE: resolve the host chain of the 1ms poller (LBP bringup) to guest functions -- names the stage that is starving. */
  `runtime/syscalls/sys_timer.c:141`

## `PPU`

- **`PPU_FORCE_READ_ADDR`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:979`
- **`PPU_FORCE_READ_VAL`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:980`
- **`PPU_GUARD_EA`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:2123`  _(+1 more sites)_
- **`PPU_HOTMAP`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:869`  _(+1 more sites)_
- **`PPU_KEEP_EA`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:2567`
- **`PPU_NOOP_BAD_VCALL`** — Opt-in (PPU_NOOP_BAD_VCALL): also no-op MISALIGNED / out-of-code-range targets. A valid entry is 4-aligned in game .text (0x10000..0xFFFFFFF) or PRX (0x30000000..0x3FFFFFFF). This lets the boot proceed PAST bad vcalls (diagnostic band-aid — skipped methods leave state incomplete). Default OFF so the boot takes a clean crash-with-backtrace instead. */
  `runtime/ppu/ppu_loader.cpp:1885`
- **`PPU_OPD_RECOVER`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1734`
- **`PPU_POKE`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:2546`
- **`PPU_POKE8`** — otherwise unanswerable without implementing a whole subsystem -- "if this flag did become what the title is waiting for, would it go anywhere?" -- and the answer decides whether the subsystem is worth building. */ PPU_POKE8=<hex ea>:<hex byte>[,...] -- the byte-wide form. A flag a title gates behaviour on is often one byte inside a struct, and a 32-bit poke would trample its neighbours. */
  `runtime/ppu/ppu_loader.cpp:2519`
- **`PPU_POKE_AFTER_MS`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:2536`
- **`PPU_RESV_OFF`** — stwcx./stdcx. store-conditional (see ppu_recomp.h). Under the reservation lock: re-validate the caller's reservation (broken by any concurrent committer), CAS iff the big-endian word still equals `expected`, and on success break every other thread's reservation on that word so their stwcx retries. */ PPU_RESV_OFF=1 -> plain value-CAS (v0 baseline, for A/B diagnosis). */
  `runtime/ppu/ppu_loader.cpp:609`
- **`PPU_RESV_STORE`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:558`
- **`PPU_RETWATCH`** — PPU_RETWATCH=<hex>: catch the first vcall(s) whose RETURN value (r3) equals this, pinning the exact virtual method that produces the poison. */
  `runtime/ppu/ppu_loader.cpp:1848`
- **`PPU_RVAL`** — PPU_RVAL=<hex>: catch where a value is READ FROM memory (its source loc) — the complement to PPU_WVAL, to find the origin of the 0xC708C708 poison. */
  `runtime/ppu/ppu_loader.cpp:1021`
- **`PPU_RVAL64`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1097`
- **`PPU_RWATCH`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:926`
- **`PPU_SAMPLE_FRAMES`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:647`
- **`PPU_SAMPLE_PRESENT`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:645`
- **`PPU_SAMPLE_RUN`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:644`
- **`PPU_SAMPLE_START`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:646`
- **`PPU_SPINBT`** — PPU_SPINBT=<addr>: one-shot host backtrace when the read32 hot spin is on the watched address -- names the guest function containing the spin loop + its callers (resolve RVAs against the linker map). Same mechanism as the read64 variant below; LBP's GPU-ref wait (0x03002008) is a read32 spin. */
  `runtime/ppu/ppu_loader.cpp:1071`  _(+1 more sites)_
- **`PPU_STUCKMAX`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1906`
- **`PPU_THREADARG`** — _(no description in source)_
  `runtime/syscalls/sys_ppu_thread.c:405`
- **`PPU_THREADGATE`** — _(no description in source)_
  `runtime/syscalls/sys_ppu_thread.c:430`
- **`PPU_TOCFIX`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1835`
- **`PPU_TOCWATCH`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1611`
- **`PPU_VCALL_BT`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1891`
- **`PPU_WVAL`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1151`
- **`PPU_WVAL_HI`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1160`
- **`PPU_WVAL_LO`** — PPU_WVAL_LO/HI: restrict to a destination range. A distinctive value is usually also a common constant -- 100.0f lands in .data dozens of times before it ever reaches the heap -- so without this the interesting hit is past the print cap before it happens. */
  `runtime/ppu/ppu_loader.cpp:1159`
- **`PPU_WWATCH`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1179`
- **`PPU_WWATCH_LEN`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1136`  _(+1 more sites)_
- **`PPU_WWATCH_MAX`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1215`
- **`PPU_WW_GUARD`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1196`

## `PS1`

- **`PS1_EVENTS`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:8728`
- **`PS1_FBDUMP`** — It used to write a 1.5 MB PPM on every heartbeat, and that instrumentation was itself most of the "nondeterminism" this port was chasing: runs carrying the periodic dump reached 0-7,127 non-zero words, while clean runs reach 75,527 every time. Measuring the thing was changing it. One dump, gated on content. */
  `libs/video/rsx_live_draw.c:8885`
- **`PS1_FBDUMP_MIN`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:8888`
- **`PS1_GP0HIST`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:8799`
- **`PS1_LOOPWATCH`** — _(no description in source)_
  `runtime/syscalls/sys_ppu_thread.c:817`
- **`PS1_PC`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:8616`
- **`PS1_PC_CENSUS`** — _(no description in source)_
  `runtime/syscalls/sys_ppu_thread.c:856`
- **`PS1_R3000_PC`** — _(no description in source)_
  `runtime/syscalls/sys_ppu_thread.c:676`
- **`PS1_RINGDUMP`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:8850`
- **`PS1_SPINWAIT`** — _(no description in source)_
  `runtime/syscalls/sys_ppu_thread.c:646`
- **`PS1_VRAM_READY`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:8863`

## `PS3`

- **`PS3_ALLOCTAG`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1287`  _(+1 more sites)_
- **`PS3_ARGV`** — emulator takes NINE, and argv[5] is the only place it learns which game to load -- with one argument it boots, believes it is emulating, and renders nothing. The layout is what lv2 does (64-bit big-endian pointer slots, NULL-terminated, then a NULL envp, strings 16-byte aligned after the slots), verified against RPCS3 ppu_load_exe. For a single argument it is byte-identical to what this produced before, so no port moves. */
  `runtime/ppu/ppu_loader.cpp:2841`
- **`PS3_BOOT_PATH`** — argv[0] MUST match the real disc path — the game's boot sequencer (func @~0x61e00) strncmp's argv[0] against "/dev_bdvd" and branches (disc vs hdd). RPCS3 oracle: real argv[0]=/dev_bdvd/PS3_GAME/USRDIR/EBOOT.BIN. The old /dev_hdd0/NPUA80001 (another game's id) failed the compare -> wrong boot branch -> init divergence/deadlock. $PS3_BOOT_PATH overrides. */
  `runtime/ppu/ppu_loader.cpp:2828`
- **`PS3_BREADCRUMB`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1608`  _(+1 more sites)_
- **`PS3_CALLTRACE`** — PS3_CALLTRACE=N: log the first N indirect calls (target + r3/r4) -- generic visibility into vtable/callback dispatch (e.g. which job body a JobManager worker runs). */
  `runtime/ppu/ppu_loader.cpp:1622`
- **`PS3_CALLTRACE_FROM`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1643`
- **`PS3_CALLTRACE_LR`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1627`
- **`PS3_CALLTRACE_R5`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1652`
- **`PS3_CALLTRACE_TO`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1639`
- **`PS3_CAMERA`** — _(no description in source)_
  `libs/hardware/cellCamera.c:58`
- **`PS3_COND_OBJ`** — _(no description in source)_
  `runtime/syscalls/sys_cond.c:172`
- **`PS3_COND_PEEK`** — _(no description in source)_
  `runtime/syscalls/sys_cond.c:189`
- **`PS3_COND_SPURIOUS_MS`** — _(no description in source)_
  `runtime/syscalls/sys_cond.c:245`
- **`PS3_DEBUG`** — _(no description in source)_
  `runtime/ppu/tests/boot_main.cpp:925`
- **`PS3_DEV_FLASH`** — _(no description in source)_
  `runtime/ppu/ppu_fs.cpp:112`  _(+1 more sites)_
- **`PS3_DISCGAME_BOOT`** — _(no description in source)_
  `libs/system/cellSysutil.c:385`
- **`PS3_EDAT`** — _(no description in source)_
  `libs/filesystem/edat.c:442`
- **`PS3_EVF_FORCE`** — PS3_EVF_FORCE (diagnostic): the game's init blocks polling event_flag (flag=100 bits=0x2) for a subsystem/SPU completion that our HLE never fires, so boot stalls on a black screen. Force-satisfy the wait (set the awaited bits) to see if the game advances into its real render/content code. Blunt; identifies the gate. */
  `runtime/syscalls/sys_event.c:1193`
- **`PS3_EVF_OK_IF_MISSING`** — PS3_EVF_OK_IF_MISSING (diagnostic, env-gated): the game busy-spins ~145k times on event_flag_wait(flag=100 bits=0x2) with a GARBAGE mode (0x38CAE4) on a NEVER-CREATED flag -> ESRCH each time. Test whether returning CELL_OK (as if the flag were set) breaks the spin and lets the game progress into real render code. Diagnostic only; identifies whether the spin is the gate. */
  `runtime/syscalls/sys_event.c:1154`
- **`PS3_EVQSTAT`** — _(no description in source)_
  `runtime/syscalls/sys_event.c:705`
- **`PS3_EVT_RECV`** — _(no description in source)_
  `runtime/syscalls/sys_event.c:592`
- **`PS3_EVT_RECV_TRACE`** — _(no description in source)_
  `runtime/syscalls/sys_event.c:569`
- **`PS3_EVT_SEND_STACK`** — _(no description in source)_
  `runtime/syscalls/sys_event.c:921`
- **`PS3_EXIT_CHAIN`** — sys_process_exit (abort path): dump the guest back-chain so we see WHO aborted. Always-on for a NONZERO exit code (error/abort) -- the rare loader-thread abort is usually a race that will not reproduce on demand, so capture it whenever it fires. Clean code=0 exits (normal shutdown) stay quiet; PS3_EXIT_CHAIN=1 dumps those too. */
  `runtime/ppu/ppu_hle.cpp:266`
- **`PS3_FRONTIER_RING`** — _(no description in source)_
  `runtime/diag/yz_frontier_trace.c:189`
- **`PS3_FRONTIER_RING_COMPACT`** — _(no description in source)_
  `runtime/diag/yz_frontier_trace.c:191`
- **`PS3_FRONTIER_RING_PARITY`** — _(no description in source)_
  `runtime/diag/yz_frontier_trace.c:192`
- **`PS3_FRONTIER_RING_PATH`** — _(no description in source)_
  `runtime/diag/yz_frontier_trace.c:190`
- **`PS3_FRONTIER_RING_TSV`** — _(no description in source)_
  `runtime/diag/yz_frontier_trace.c:223`
- **`PS3_FSLOG`** — _(no description in source)_
  `runtime/ppu/ppu_fs.cpp:398`  _(+8 more sites)_
- **`PS3_FSLOG_BT`** — _(no description in source)_
  `runtime/ppu/ppu_fs.cpp:243`  _(+2 more sites)_
- **`PS3_FSLOG_EOF`** — _(no description in source)_
  `runtime/ppu/ppu_fs.cpp:381`
- **`PS3_FSLOG_READS`** — _(no description in source)_
  `runtime/ppu/ppu_fs.cpp:335`  _(+2 more sites)_
- **`PS3_FSLOG_TOC`** — data.toc read -> who parses it?
  `runtime/ppu/ppu_fs.cpp:421`
- **`PS3_FSTRACE`** — _(no description in source)_
  `runtime/syscalls/sys_fs.c:458`
- **`PS3_GUEST_PROF`** — _(no description in source)_
  `runtime/ppu/tests/boot_main.cpp:919`
- **`PS3_HDD0_ROOT`** — _(no description in source)_
  `runtime/ppu/ppu_fs.cpp:99`  _(+1 more sites)_
- **`PS3_HLE_ARGS`** — _(no description in source)_
  `runtime/ppu/ppu_hle.cpp:249`
- **`PS3_HLE_PTRCHECK`** — PS3_HLE_PTRCHECK: the guest stores HLE return values as guest pointers, so an HLE returning a value outside guest RAM (>= 0x10000000) that is not a CELL_ERROR code (0x8001xxxx / 0x8002xxxx) is leaking a truncated HOST pointer. It surfaces much later as an unresolved indirect call with no hint of where it came from, which is why it is worth checking for directly. */
  `runtime/ppu/ppu_hle.cpp:690`
- **`PS3_HLE_TRACE`** — _(no description in source)_
  `runtime/ppu/ppu_hle.cpp:236`
- **`PS3_HLE_UNRESOLVED`** — _(no description in source)_
  `runtime/ppu/ppu_hle.cpp:143`
- **`PS3_LWMUTEX_TRACE`** — PS3_LWMUTEX_TRACE=1: reconstruct the lwmutex lock-convoy that stalls LBP's loader. Records, per mutex, the acquiring tid + a QPC microsecond timestamp; on a contended block it logs who holds it and for how long; on unlock it flags a long hold. The leaf holder (the one blocked on a non-lwmutex wait) is the convoy root. Default OFF. */
  `runtime/ppu/ppu_sysprx.cpp:162`
- **`PS3_MEMTRACE`** — _(no description in source)_
  `runtime/ppu/ppu_sysprx.cpp:420`  _(+1 more sites)_
- **`PS3_MILESTONE_OUT`** — _(no description in source)_
  `runtime/milestone.c:81`
- **`PS3_NET_ONLINE`** — _(no description in source)_
  `libs/network/cellNetCtl.c:47`
- **`PS3_NO_AUDIO`** — PS3_NO_AUDIO=1: report that the audio library is unavailable. Middleware that drives its mixer through SPU jobs will otherwise block the whole boot waiting on work we cannot yet complete; failing here lets a title take its silent path and keep going. Diagnostic first, workaround second. */
  `libs/audio/cellAudio.c:606`
- **`PS3_NO_GUEST_PTR_TRAP`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:108`
- **`PS3_NO_JOBCHAIN`** — _(no description in source)_
  `libs/spurs/cellSpurs.c:2499`
- **`PS3_NO_SAIL`** — PS3_NO_SAIL=1: report that the player cannot be created. On hardware this call spawns the SAIL player's own worker threads, and a title that then waits for one of them to signal readiness will wait forever against a stub that spawns nothing. Failing cleanly lets a title take its "no video" path instead of deadlocking during setup. */
  `libs/codec/cellSail.c:482`
- **`PS3_NO_SPURS_PM`** — _(no description in source)_
  `libs/spurs/cellSpurs.c:1438`
- **`PS3_NO_THREAD_EXIT_UNWIND`** — _(no description in source)_
  `runtime/syscalls/sys_ppu_thread.c:497`
- **`PS3_PRX_CALLTRACE`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1784`
- **`PS3_SAMPLE`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:483`
- **`PS3_SAVEDATA_ARG9_OFF`** — _(no description in source)_
  `libs/system/cellSaveData.c:1317`
- **`PS3_SCBLOCK_PROF`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:2365`
- **`PS3_SCENTER`** — PS3_SCENTER=1: log the syscall BEFORE dispatching it. sc_trace runs after the call returns, so a syscall that never returns leaves no trace at all -- which is exactly the case being chased here: the main guest thread runs ~42,000 R3000 instructions in 30 ms, then parks in ntdll forever with no further [sc] line. The last ENTER without a matching exit names the call that blocked. */
  `runtime/ppu/ppu_loader.cpp:2358`
- **`PS3_SCTRACE`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:140`
- **`PS3_SCTRACE_TID`** — PS3_SCTRACE_TID: trace every lv2 syscall made by the loader/worker thread (tid=1) so we can see what it does AFTER receiving its q=1 event and why it never registers handlers / loads assets. */
  `runtime/ppu/ppu_loader.cpp:2149`
- **`PS3_SDK_VERSION`** — _(no description in source)_
  `runtime/syscalls/lv2_register.c:1866`
- **`PS3_SEMTRACE`** — _(no description in source)_
  `runtime/syscalls/sys_semaphore.c:540`
- **`PS3_SYNCLOG`** — PS3_SYNCLOG: which guest object each cond id belongs to, and who made it. A deadlock report names a cond by id; without this there is no way back from "nothing signals cond 3" to the subsystem that owns cond 3. */
  `runtime/syscalls/sys_cond.c:121`
- **`PS3_TITLE`** — Window caption: $PS3_TITLE wins, else the TITLE field PARAM.SFO already gave cellGame, else a neutral name. It used to fall back to a specific other title, so every port announced itself as that game. */
  `runtime/ppu/tests/boot_main.cpp:343`
- **`PS3_USRDIR_BASE`** — PS3_USRDIR_BASE: a title installed as a full /dev_hdd0/game/<ID> tree keeps its USRDIR there, not at the vfs root, so the flattening above sends it to a directory that does not exist. ps1_netemu writes its settings with a bare "/USRDIR/CONFIG" and read them back the same way; both missed, and it printed "save config file: /USRDIR/CONFIG" / "failed" on every boot. Opt-in so flattened trees keep the old path. */
  `runtime/syscalls/sys_fs.c:175`
- **`PS3_VERBOSE`** — _(no description in source)_
  `runtime/ps3_log.h:41`
- **`PS3_VFS_ROOT`** — _(no description in source)_
  `runtime/ppu/tests/boot_main.cpp:147`  _(+1 more sites)_
- **`PS3_WAITBT`** — PS3_WAITBT: one-shot guest-stack dump per (tid,queue) so we can name the exact game function the main thread is stuck polling in the flip loop. */
  `runtime/syscalls/sys_event.c:301`
- **`PS3_WAIT_OBJ`** — _(no description in source)_
  `runtime/syscalls/sys_timer.c:119`

## `PS3RECOMP`

- **`PS3RECOMP_METAL_HEADLESS`** — _(no description in source)_
  `libs/video/rsx_metal_backend.m:461`

## `PSOMISSDBG`

- **`PSOMISSDBG`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:2033`

## `PT`

- **`PT`** — PT detector: does this byte-write turn its word into the hunted truncated value? */
  `runtime/ppu/ppu_loader.cpp:1245`

## `RD`

- **`RD_SPU_DONE_EVENT`** — EXPERIMENT (RD_SPU_DONE_EVENT): on SPU thread completion, deliver an event to the connected queue -- PPU code may block in sys_event_queue_receive for the SPU's completion signal (which normally comes from a WrOutIntrMbox the SPU issues before finishing). Tests whether the render hang is that wait. */
  `runtime/syscalls/lv2_register.c:634`
- **`RD_SPU_FRAME_MBOX`** — _(no description in source)_
  `runtime/syscalls/lv2_register.c:784`
- **`RD_SPU_INTERP`** — _(no description in source)_
  `runtime/syscalls/lv2_register.c:722`  _(+1 more sites)_
- **`RD_SPU_INTERP_ASYNC`** — exists so such a worker does its full init + ready handshake and then PARKS at its first idle mailbox poll -- the whole point being that no async host thread races the PPU. Spawning one anyway meant the group_start run and a write_spu_mb re-run competed for the same mailbox word: one run consumed it, the other polled an empty box and parked, and which got it varied run to run. */
  `runtime/syscalls/lv2_register.c:927`

## `REMAPDBG`

- **`REMAPDBG`** — D3D12 mapping: destR | destG<<3 | destB<<6 | destA<<9 | valid bit */
  `libs/video/rsx_d3d12_backend.c:2409`

## `RESV`

- **`RESV_DIAG`** — RESV_DIAG: detect a stwcx whose ctx isn't in the registry -- if that fires, that thread's reservations are never broken by others (ABA slips through), and the fix is to register it (not plain-store invalidation). One line per ctx. */
  `runtime/ppu/ppu_loader.cpp:591`

## `RSX`

- **`RSX_A010_CAPTURE_ONCE`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:8954`
- **`RSX_A010_GEOM`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:5637`
- **`RSX_A010_PROBE`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:4676`  _(+2 more sites)_
- **`RSX_A010_SURFACE_DUMP`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:4677`  _(+1 more sites)_
- **`RSX_A010_SURFACE_EVERY`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:8938`
- **`RSX_ACCUM_FRAME`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:5258`
- **`RSX_CAP`** — rsxcap: snapshot whole frames (backbuffer + every offscreen RT + a draw manifest) at RSX_CAP=start[:count[:stride]] frame_counts. Runs before the present transition would matter -- the backbuffer is in PRESENT here and is restored to PRESENT after each readback. */
  `libs/video/rsx_d3d12_backend.c:5034`
- **`RSX_CAP_DIR`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:1474`
- **`RSX_CUBE_TEX`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:1954`
- **`RSX_D3D_DEBUG`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:861`
- **`RSX_DBG_CB`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:3132`
- **`RSX_DBG_PICK`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:5788`
- **`RSX_DBG_RECENTER`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:5834`
- **`RSX_DBG_SCALE`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:5818`
- **`RSX_DBG_SHIFT`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:5850`
- **`RSX_DBG_TRACK`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:4741`  _(+1 more sites)_
- **`RSX_DBG_VP`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:1759`
- **`RSX_DBG_VTX`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:5618`  _(+3 more sites)_
- **`RSX_DRAW_CSV`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:5541`
- **`RSX_DRAW_CSV_A010_ONLY`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:5533`
- **`RSX_DUMP_EVERY`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:8922`
- **`RSX_FP_CONSTANT_MODE`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:7048`
- **`RSX_FP_CTRL_AUTO`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:3894`
- **`RSX_H`** — _(no description in source)_
  `runtime/ppu/tests/boot_main.cpp:354`
- **`RSX_LIVE_DRAW`** — Opt-in in this tree. Upstream (caner's Yakuza fork) defaults this ON because that runner has no other renderer; here rsx_d3d12_backend.c is still the default for every other title, so the live engine has to be asked for. (The YZ_RSX_DRAW alias his scripts used went with the title-hack drain; RSX_LIVE_DRAW is the one spelling.) */
  `libs/video/rsx_live_draw.c:1229`
- **`RSX_LIVE_FEED_DBG`** — _(no description in source)_
  `libs/video/cellGcmSys.c:1576`  _(+2 more sites)_
- **`RSX_MOVEMENT_PROBE_INTERVAL_MS`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:8400`
- **`RSX_MOVEMENT_PROOF`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:8380`
- **`RSX_MOVEMENT_PROOF_DELAY_MS`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:8399`
- **`RSX_MOVIE_COMPOSITE_UI`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:5180`
- **`RSX_MOVIE_TRACK`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:7392`  _(+1 more sites)_
- **`RSX_NO_RESTART`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:4427`  _(+1 more sites)_
- **`RSX_NO_SHADER_DISK_CACHE`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:3163`
- **`RSX_NO_TEX_REFRESH`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:2379`
- **`RSX_NO_ZETA_TRACK`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:2880`
- **`RSX_PARITY_DIAG`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:8260`
- **`RSX_SCHEDULE_DIAG`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:582`
- **`RSX_SHADER_CACHE_DIR`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:3168`
- **`RSX_TARGET_TRACE`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:5043`
- **`RSX_TRACE`** — RSX_TRACE=<N>: trace the first N methods (bare "1" keeps the old 250). The fixed 250 was spent entirely on boot-time setup, so the methods around the first real draw -- exactly the ones worth seeing -- were never traced. */
  `libs/video/rsx_commands.c:295`
- **`RSX_VALIDATION_CAPTURE`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:8494`
- **`RSX_VALIDATION_DIR`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:8261`  _(+2 more sites)_
- **`RSX_VERTEX_DIAG_DIR`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:7111`
- **`RSX_VERTEX_MODE`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:7074`
- **`RSX_VERTEX_PATH`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:7075`
- **`RSX_VERTEX_STATE_TRACE`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:7358`
- **`RSX_VTEX_TRACE`** — _(no description in source)_
  `libs/video/rsx_live_draw.c:2576`
- **`RSX_VTXDBG`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:6493`
- **`RSX_W`** — _(no description in source)_
  `runtime/ppu/tests/boot_main.cpp:353`

## `RT`

- **`RT_CLEARDBG`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:4481`  _(+1 more sites)_
- **`RT_DISPLAY_BY_SIZE`** — cellGcmSetDisplayBuffer only registers the buffers the FLIP may point at; a guest is free to render into a different surface of the same size and flip to it later (PSGL does). Matching offsets alone then classifies every draw as offscreen, has_display stays 0, and render_frame() -- which is what draws ALL recorded geometry -- is never called. The backbuffer shows only the clear, which looks exactly like "nothing rasterizes". */
  `libs/video/rsx_d3d12_backend.c:3242`
- **`RT_FORCE_RGBA8`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:3290`
- **`RT_OFFDBG`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:3222`
- **`RT_REBIND`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:4621`
- **`RT_SEQDBG`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:3217`

## `RTT`

- **`RTT_DUMP`** — _(no description in source)_
  `libs/video/cellGcmSys.c:1600`  _(+5 more sites)_
- **`RTT_PASS`** — Debug: RTT_PASS=N shows pass N's output directly on screen (drops later ops, retargets pass N to the backbuffer). */
  `libs/video/rsx_d3d12_backend.c:3908`
- **`RTT_SAVEA`** — RTT_SAVEA=1: show the alpha lane in RED (gates like wave's mask.w live there). */
  `libs/video/rsx_d3d12_backend.c:4996`
- **`RTT_SAVERT`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:4767`
- **`RTT_SAVERT_AFTER`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:4777`
- **`RTT_SAVERT_SKIP`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:4770`
- **`RTT_VIEWRT`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:3911`  _(+1 more sites)_

## `SCREEN`

- **`SCREEN_AS_TEX`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:3516`  _(+1 more sites)_

## `SCREENCOPY`

- **`SCREENCOPY_DUMP`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:3558`

## `SCREENTEX`

- **`SCREENTEX`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:4111`

## `SEM`

- **`SEM_BADID`** — _(no description in source)_
  `runtime/syscalls/sys_semaphore.c:200`
- **`SEM_ERRDBG`** — _(no description in source)_
  `runtime/syscalls/sys_semaphore.c:332`
- **`SEM_WAITDBG`** — _(no description in source)_
  `runtime/syscalls/sys_semaphore.c:340`

## `SEMCHAIN`

- **`SEMCHAIN`** — SEMCHAIN: dump the guest call-chain at the main thread's sem=7 poll (the "loading done" wait) to locate its loop -- is it meant to re-post sem=3? */
  `runtime/syscalls/sys_semaphore.c:365`  _(+1 more sites)_

## `SEMTID`

- **`SEMTID`** — _(no description in source)_
  `runtime/syscalls/sys_semaphore.c:351`  _(+3 more sites)_

## `SEQ`

- **`SEQ_DBG`** — _(no description in source)_
  `libs/video/rsx_commands.c:605`  _(+1 more sites)_

## `SPU`

- **`SPU_ARGS_DUMP`** — _(no description in source)_
  `runtime/syscalls/lv2_register.c:699`
- **`SPU_ARGWATCH`** — _(no description in source)_
  `runtime/spu/spu_channels.c:181`
- **`SPU_ATOMTRACE`** — _(no description in source)_
  `runtime/spu/spu_channels.c:300`  _(+2 more sites)_
- **`SPU_ATOM_EA`** — _(no description in source)_
  `runtime/spu/spu_channels.c:356`
- **`SPU_ATOM_FULL`** — _(no description in source)_
  `runtime/spu/spu_channels.c:366`
- **`SPU_CALLTRACE`** — _(no description in source)_
  `runtime/spu/spu_interp.c:329`
- **`SPU_CHHIST`** — SPU_CHHIST=1: which channels a job actually touches, and how often. A worker that never issues an MFC command is either not being handed its work descriptor or is waiting on a channel we never satisfy; the channel mix distinguishes those. */
  `runtime/spu/spu_channels.c:564`  _(+2 more sites)_
- **`SPU_CH_BLOCK`** — _(no description in source)_
  `runtime/spu/spu_channels.c:652`
- **`SPU_CMDDUMP`** — SPU_CMDDUMP=1: decode the FULL WWS command list (8-byte cmds). Each CommandFlags u32: commandNum=(f>>9)&7, outputShareable=(f>>13)&1, shareableWriteIfDiscarded=(f>>12)&1, logBufSet=(f>>5)&0xF, logBuf=f&0x1F. Answers lifter-vs-builder: are shareable-output UseBuffer + kRunJob + store commands PRESENT in the list the PPU built? */
  `runtime/spu/spu_dma.h:1319`
- **`SPU_CMDHIST`** — SPU_CMDHIST=1: every MFC command that reaches the engine, by opcode and SPU image. Catches list DMAs (putl/getl) that never reach mfc_do_transfer's per-element path. */
  `runtime/spu/spu_dma.h:789`
- **`SPU_DBG_MBOX`** — LS work buffer here (LS 0x11D4) and then poll it, and the guest sets class-2 mask 0x3 -- which excludes the plain-mailbox bit 0x10 -- so this write raises no interrupt and the PPU must POLL to see it. If the depth climbs and stays, nobody is polling and the message is stranded. That is the difference between "the PPU is slow" and "the PPU never looks", which no other probe here distinguishes. */
  `runtime/spu/spu_channels.c:603`  _(+1 more sites)_
- **`SPU_DMACHK`** — SPU_DMACHK=1: report transfers that break the MFC rules the guest's own dma.h asserts on -- size 0 or > 16 KB, size not a multiple of 16 for transfers of 16+ bytes, or LSA/EA not sharing 16-byte alignment. */
  `runtime/spu/spu_dma.h:242`
- **`SPU_DMATRACE`** — _(no description in source)_
  `runtime/spu/spu_dma.h:1134`
- **`SPU_DMATRACE_ALL`** — _(no description in source)_
  `runtime/spu/spu_dma.h:432`  _(+2 more sites)_
- **`SPU_DMATRACE_RAW`** — _(no description in source)_
  `runtime/spu/spu_dma.h:1193`
- **`SPU_DMA_LAX`** — the guest image. YDKJ issues exactly one such PUT -- 16 KB from a 4-byte-aligned EA of 0x00542004, straight over the title's own data -- and every later call through the clobbered OPDs reports "code 0x00000000 not registered". SPU_DMA_LAX=1 restores the old permissive behaviour. */
  `runtime/spu/spu_dma.h:215`
- **`SPU_DMA_REPEAT_LIMIT`** — _(no description in source)_
  `runtime/spu/spu_dma.h:1099`
- **`SPU_DMA_SKIP_DUMP`** — SPU_DMA_SKIP_DUMP=1: show the LS payload a skipped PUT was carrying. A job that DMAs a small buffer to a garbage EA in a tight loop is usually an SPU-side assert/print path, and the payload names the actual complaint -- far more useful than the address it failed at. */
  `runtime/spu/spu_dma.h:392`
- **`SPU_DMA_WATCH`** — SPU_DMA_WATCH=<hex>: report any transfer whose destination RANGE covers this guest address. A DMA landing in the loaded ELF image is always a bug, and it is invisible to the sampling traces -- YDKJ had an SPU job writing over the title's own OPD table at 0x00530D78, which turned every call through it into "code 0x00000000 not registered". */
  `runtime/spu/spu_dma.h:192`
- **`SPU_DSPDESC`** — _(no description in source)_
  `runtime/spu/spu_channels.c:2062`
- **`SPU_DSP_IMAGE_EA`** — _(no description in source)_
  `runtime/spu/spu_dma.h:500`
- **`SPU_DUMP_GET`** — _(no description in source)_
  `runtime/spu/spu_dma.h:473`
- **`SPU_DUMP_IMPORT`** — SPU_DUMP_IMPORT=<dir>: save each unique imported ELF (FMOD's runtime- materialized SPU overlay plugins) so they can be lifted + registered. Extent = max(p_off+p_fsz) over PT_LOADs, re-walked here cheaply. */
  `runtime/syscalls/lv2_register.c:1688`
- **`SPU_DUMP_MISS`** — SPU_DUMP_MISS=<dir>: write the unrecognised image out so it can be lifted and registered. SPURS job binaries are raw code+data blobs the title loads from its own data files -- unlike sys_spu_image programs they are NOT embedded ELFs in the EBOOT, so tools/extract_spu_images.py cannot find them and this is the only place the bytes exist. One file per fingerprint. */
  `runtime/spu/spu_workload.c:734`
- **`SPU_EXITTRACE`** — _(no description in source)_
  `runtime/spu/spu_drain.c:56`
- **`SPU_GETHIST`** — SPU_PUTHIST=1: histogram of PUT destinations by 1 MB bucket. "the SPU ran 112k instructions" does not say whether its results reached the buffer the RSX reads; this says where they actually went. */ SPU_GETHIST=1: the same accounting for GETs, on the SOURCE side. A solver whose inputs read back as zeros produces zeros no matter how many instructions it runs. */
  `runtime/spu/spu_dma.h:256`
- **`SPU_H2LSRC`** — _(no description in source)_
  `runtime/spu/spu_dma.h:983`
- **`SPU_HOST_DEPTH_MAX`** — _(no description in source)_
  `runtime/spu/spu_drain.c:308`
- **`SPU_HOST_STACK_BIG`** — Lifted SPU loops can become deep C tail-call recursion; give SPU host threads a large stack (reserved). Bumped to 512 MB to diagnose whether the cri/taskset-policy dispatch chain overflows (env SPU_HOST_STACK_BIG). */
  `runtime/syscalls/lv2_register.c:942`
- **`SPU_IBCOV`** — SPU_IBCOV: image-3 (Bink SPU) PC-page coverage. Track which 0x1000-byte LS pages the task's indirect branches land in; dump the set periodically. If coverage stays in the kernel/wait region (~0x13xxx) the decode routine never runs; if it spans a wide high range, decode executes but doesn't output. */
  `runtime/spu/spu_channels.c:1622`
- **`SPU_IMAGE_DIAG`** — SPU_IMAGE_DIAG=1: r6 is a caller-supplied guest buffer that differs on every call, which this handler ignores -- it points the segments at the source image instead. Show the segments written and what the caller's buffer holds, to establish whether the caller expects it to be filled. */
  `runtime/ppu/ppu_sysprx.cpp:701`
- **`SPU_IMG_DUMP`** — _(no description in source)_
  `runtime/syscalls/lv2_register.c:573`
- **`SPU_INTERP_UNLIFTED`** — _(no description in source)_
  `runtime/spu/spu_channels.c:1767`
- **`SPU_IRQTRACE`** — _(no description in source)_
  `runtime/spu/spu_channels.c:1548`  _(+1 more sites)_
- **`SPU_JOBDRAIN`** — _(no description in source)_
  `runtime/spu/spu_drain.c:101`
- **`SPU_JOBEXEC`** — _(no description in source)_
  `runtime/spu/spu_channels.c:1934`
- **`SPU_JOBEXEC_DUMP`** — _(no description in source)_
  `runtime/spu/spu_channels.c:1945`
- **`SPU_JOBTRACE`** — _(no description in source)_
  `runtime/spu/spu_channels.c:1864`
- **`SPU_LLARWATCH`** — _(no description in source)_
  `runtime/spu/spu_channels.c:452`
- **`SPU_LOCKSTEP`** — _(no description in source)_
  `runtime/spu/spu_lockstep.c:82`
- **`SPU_LOCKSTEP_QUANTUM`** — _(no description in source)_
  `runtime/spu/spu_lockstep.c:85`
- **`SPU_LSDUMP`** — _(no description in source)_
  `runtime/spu/spu_channels.c:863`  _(+1 more sites)_
- **`SPU_LSREAD_TRACE`** — _(no description in source)_
  `runtime/syscalls/lv2_register.c:1479`  _(+1 more sites)_
- **`SPU_LS_DUMP`** — SPU_LS_DUMP=<prefix>: write this job's whole local store when it ends. A reference dump from real hardware is a full local store, so the only way to ask "what ELSE is different" -- rather than checking one guessed word at a time -- is to diff all 256 KB. */
  `runtime/spu/spu_workload.c:679`
- **`SPU_LS_LOWREAD`** — _(no description in source)_
  `runtime/spu/spu_context.h:414`
- **`SPU_LS_WATCH`** — _(no description in source)_
  `runtime/spu/spu_context.h:46`
- **`SPU_MBOXTRACE`** — _(no description in source)_
  `runtime/spu/spu_channels.c:607`  _(+2 more sites)_
- **`SPU_MFC_TRACE`** — SPU_MFC_TRACE: attribute silent DMA-poll loops (a wedged task whose host thread samples "in ntdll" because VirtualQuery dominates). Prints every 64k-th transfer per thread: enough to see the loop's pc/ea. */
  `runtime/spu/spu_dma.h:308`  _(+1 more sites)_
- **`SPU_MISS_DUMP_IMG`** — _(no description in source)_
  `runtime/spu/spu_channels.c:2075`
- **`SPU_MISS_DUMP_PCMIN`** — _(no description in source)_
  `runtime/spu/spu_channels.c:2076`
- **`SPU_NO_LS0_END`** — _(no description in source)_
  `runtime/spu/spu_drain.c:50`
- **`SPU_OVL_DIAG`** — LBP overlay-load diagnosis: a GET of the FMOD overlay descriptor (32B from ~0x94Fxxx) then a large GET from a NULL source = the overlay never loads. Dump the descriptor content to see where the real source EA was dropped. */
  `runtime/spu/spu_dma.h:535`
- **`SPU_PCHIST`** — _(no description in source)_
  `runtime/spu/spu_context.h:662`
- **`SPU_POLLTRACE`** — _(no description in source)_
  `runtime/spu/spu_channels.c:289`  _(+3 more sites)_
- **`SPU_PUTEA`** — SPU_PUTEA=1: the first few FULL destination addresses per SPU. The 1 MB bucket histogram showed spu1..3 writing only bucket 0 and spu0 buckets 1..3, with nothing in bucket 4 -- where the composite samples PS1 VRAM (RSX local + 0x400000). Buckets keep only 4 address bits, so that may be an artifact of where RSX local memory actually sits; the full EA settles it. */
  `runtime/spu/spu_dma.h:1011`
- **`SPU_PUTHIST`** — _(no description in source)_
  `runtime/spu/spu_dma.h:266`  _(+1 more sites)_
- **`SPU_RAW_DBG`** — _(no description in source)_
  `runtime/spu/spu_raw.c:93`
- **`SPU_RAW_DUMP`** — SPU_RAW_DUMP=<path>: write the image exactly as the guest presents it, so a fingerprint that does not match the registered one can be diffed against the file the offline extractor produced. */
  `runtime/spu/spu_raw.c:202`
- **`SPU_ROWCOV`** — _(no description in source)_
  `runtime/spu/spu_dma.h:882`
- **`SPU_SEED_DBG`** — _(no description in source)_
  `runtime/syscalls/lv2_register.c:774`
- **`SPU_SIGSTAT`** — _(no description in source)_
  `runtime/spu/spu_raw.c:355`
- **`SPU_SMC_HI`** — _(no description in source)_
  `runtime/spu/spu_context.h:524`  _(+1 more sites)_
- **`SPU_SMC_LO`** — _(no description in source)_
  `runtime/spu/spu_context.h:523`  _(+1 more sites)_
- **`SPU_SMC_WATCH`** — _(no description in source)_
  `runtime/spu/spu_context.h:521`  _(+1 more sites)_
- **`SPU_SN_DEFER`** — _(no description in source)_
  `runtime/spu/spu_drain.c:181`
- **`SPU_SPEED`** — _(no description in source)_
  `runtime/syscalls/lv2_register.c:785`
- **`SPU_STACKEA_WATCH`** — SPU job a completion-word EA (param[4]) that lives on the PPU stack (0xD00Cxxxx = VM_STACK_BASE region); the job must DMA a nonzero word there on completion. This catches (a) any DMA whose EA targets the stack region -- a completion-write attempt -- and (b) any GET payload carrying a 0xD00Cxxxx dword -- the completion EA arriving in a loaded job buffer. Answers whether the EA ever reaches / is ever written by the SPU. */
  `runtime/spu/spu_dma.h:412`  _(+1 more sites)_
- **`SPU_STEPCAP`** — _(no description in source)_
  `runtime/spu/spu_interp.c:345`
- **`SPU_STEPTRACE`** — _(no description in source)_
  `runtime/spu/spu_drain.c:81`
- **`SPU_STEPTRACE_FROM`** — _(no description in source)_
  `runtime/spu/spu_drain.c:83`
- **`SPU_TAGPROBE`** — _(no description in source)_
  `runtime/spu/spu_dma.h:1440`
- **`SPU_TAILRET`** — _(no description in source)_
  `runtime/spu/spu_drain.c:343`
- **`SPU_TRACE`** — _(no description in source)_
  `runtime/spu/spu_channels.c:2186`
- **`SPU_TRACE_AT_WALL`** — _(no description in source)_
  `runtime/spu/spu_channels.c:2166`
- **`SPU_TRACE_FILE`** — _(no description in source)_
  `runtime/spu/spu_channels.c:2185`
- **`SPU_TRACE_FLOWCTX`** — _(no description in source)_
  `runtime/spu/spu_channels.c:2173`
- **`SPU_TRACE_IMG`** — _(no description in source)_
  `runtime/spu/spu_channels.c:2179`
- **`SPU_TRACE_MAX`** — _(no description in source)_
  `runtime/spu/spu_channels.c:2188`
- **`SPU_VRAMPC`** — _(no description in source)_
  `runtime/spu/spu_dma.h:926`
- **`SPU_WATCHEA`** — _(no description in source)_
  `runtime/spu/spu_dma.h:819`
- **`SPU_WATCHEA_EVERY`** — _(no description in source)_
  `runtime/spu/spu_dma.h:829`
- **`SPU_WATCHLSA`** — _(no description in source)_
  `runtime/spu/spu_dma.h:855`
- **`SPU_WHOPOLLS`** — _(no description in source)_
  `runtime/spu/spu_channels.c:841`
- **`SPU_WORKDESC_DUMP`** — buffer left untouched. Seeding only a real descriptor keeps the completion handshake intact and gives the workers their input. */ SPU_WORKDESC_DUMP=1: the work descriptor the PPU hands the worker. Its pointers are where the job DMAs from and to, so a job that writes only scratch is either reading the wrong descriptor or the descriptor does not name the buffer we expect. */
  `runtime/syscalls/lv2_register.c:759`
- **`SPU_WORKDESC_HDR`** — _(no description in source)_
  `runtime/syscalls/lv2_register.c:768`
- **`SPU_WORKER_TRACE`** — Raw-worker run summary (env SPU_WORKER_TRACE). A worker that produces no outbound word leaves the PPU blocked in sys_event_queue_receive forever, and the difference between "parked at its idle poll" and "fell over after three instructions" is invisible without this. */
  `runtime/spu/spu_lifted_job.h:289`

## `SPURS`

- **`SPURS_EF_FORCE`** — _(no description in source)_
  `libs/spurs/cellSpurs.c:1801`
- **`SPURS_EF_OBJ_DELIVER`** — _(no description in source)_
  `runtime/spu/spu_channels.c:1247`  _(+1 more sites)_
- **`SPURS_EF_SPU_REPLY`** — _(no description in source)_
  `runtime/spu/spu_channels.c:1347`
- **`SPURS_EF_WAKE_PARKED`** — _(no description in source)_
  `libs/spurs/cellSpurs.c:310`
- **`SPURS_EVENT_D3`** — _(no description in source)_
  `libs/spurs/cellSpurs.c:2350`
- **`SPURS_FORCE_SPUS`** — _(no description in source)_
  `libs/spurs/cellSpurs.c:1595`
- **`SPURS_JC_DELAY`** — TIMING PROBE (SPURS_JC_DELAY=ms): the real jm2 chain walker is async and picks up jobs as the PPU appends them + fills their descriptors. Our walk is one-shot; if it reads descriptors before the PPU populates the I/O (n_dma=0, empty ioBuffer), deferring the walk should let real I/O appear. Confirms timing-vs-never before committing to the async rewrite. */
  `libs/spurs/cellSpurs.c:2471`
- **`SPURS_JOB_DESCDUMP`** — _(no description in source)_
  `runtime/spu/spurs_job.c:270`  _(+1 more sites)_
- **`SPURS_JOB_DUMP`** — _(no description in source)_
  `runtime/spu/spurs_job.c:300`  _(+1 more sites)_
- **`SPURS_JOB_MBOX`** — _(no description in source)_
  `runtime/spu/spurs_job.c:376`
- **`SPURS_KERN_WATCH`** — _(no description in source)_
  `libs/spurs/cellSpurs.c:1454`
- **`SPURS_PM_CLAIMLOG`** — _(no description in source)_
  `runtime/spu/spurs_policy.c:254`
- **`SPURS_PM_DUMP`** — PM-COMPLETENESS PROBE (SPURS_PM_DUMP): the wwsjob job-manager PM is ASSEMBLED at runtime -- the embedded ELF (LS 0xA00) has zero HOLES at LS 0xAEE..0x11B0 and the executeStage lives at LS 0x14f4, both filled by SPURS setup. Report whether OUR runtime's PM has that code or the holes, and (once) write the whole image out so we can re-lift it. */
  `libs/spurs/cellSpurs.c:1268`
- **`SPURS_PM_FLOW`** — _(no description in source)_
  `runtime/spu/spurs_policy.c:189`
- **`SPURS_PM_FLOW_ALL`** — _(no description in source)_
  `runtime/spu/spurs_policy.c:204`
- **`SPURS_PM_FRESH`** — _(no description in source)_
  `runtime/spu/spurs_policy.c:74`
- **`SPURS_PM_PIPE`** — _(no description in source)_
  `runtime/spu/spurs_policy.c:234`
- **`SPURS_PM_TRACE`** — _(no description in source)_
  `runtime/spu/spurs_policy.c:168`  _(+1 more sites)_
- **`SPURS_QUEUE_PUSH`** — _(no description in source)_
  `libs/spurs/cellSpurs.c:2661`
- **`SPURS_TASKATTR_DESC`** — _(no description in source)_
  `libs/spurs/cellSpurs.c:1006`
- **`SPURS_TASKATTR_LEGACY`** — _(no description in source)_
  `libs/spurs/cellSpurs.c:979`
- **`SPURS_TASKSET_TRACE`** — _(no description in source)_
  `runtime/spu/spu_lifted_job.h:275`  _(+1 more sites)_
- **`SPURS_TRACE`** — _(no description in source)_
  `runtime/ppu/ppu_hle.cpp:332`  _(+5 more sites)_

## `SUBVP`

- **`SUBVP_DBG`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:448`  _(+1 more sites)_
- **`SUBVP_RTT`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:402`

## `SURFDBG`

- **`SURFDBG`** — _(no description in source)_
  `libs/video/rsx_commands.c:131`

## `SYNC`

- **`SYNC_TRACE`** — ponytail: the sibling primitives below (barrier / rwlock / queue) still assume host-endian words. Same treatment when a title actually exercises them; not fixed blind here. */
  `libs/sync/cellSync.c:50`

## `SYS`

- **`SYS_HEAP_DBG`** — _(no description in source)_
  `libs/system/sysPrxForUser.c:831`  _(+1 more sites)_
- **`SYS_RSX_DBG`** — _(no description in source)_
  `libs/video/sys_rsx.c:141`
- **`SYS_RSX_DMACTL_OFF`** — _(no description in source)_
  `libs/video/sys_rsx.c:294`

## `TCONST`

- **`TCONST_ALL`** — _(no description in source)_
  `libs/video/rsx_commands.c:602`
- **`TCONST_DBG`** — _(no description in source)_
  `libs/video/rsx_commands.c:599`
- **`TCONST_FIFO`** — _(no description in source)_
  `libs/video/cellGcmSys.c:1457`
- **`TCONST_MAX`** — _(no description in source)_
  `libs/video/rsx_commands.c:601`

## `TEX`

- **`TEX_AVGDBG`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:2868`
- **`TEX_BINDDBG`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:4211`
- **`TEX_BUDGET`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:2448`
- **`TEX_HILITE`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:2911`
- **`TEX_MARKEMPTY`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:2939`
- **`TEX_NOCACHE`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:2495`
- **`TEX_OFF_BIAS`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:2519`
- **`TEX_PROBE`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:6429`
- **`TEX_REMAP`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:2544`  _(+1 more sites)_
- **`TEX_REMAP_ID`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:2404`
- **`TEX_RESOLVE_AUTO`** — TEX_RESOLVE_AUTO=1: resolve through the page tables (local-page map then IO table) instead of trusting the format's location bits. A texture the guest built in main memory but tagged local resolves to untouched VRAM and samples as all-zero -- geometry renders, flat. */
  `libs/video/rsx_d3d12_backend.c:6417`
- **`TEX_RGBA`** — _(no description in source)_
  `libs/video/rsx_texture_layout.c:77`
- **`TEX_SAVE`** — TEX_SAVE=1: also dump wide B8 uploads (Bink video planes are B8 -- the Y plane IS the movie frame in grayscale) + a content stat, so "is the decoder producing pixels" is answerable by looking at a BMP. */
  `libs/video/rsx_d3d12_backend.c:2786`  _(+1 more sites)_
- **`TEX_SRCDBG`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:2565`

## `TEXFMTDBG`

- **`TEXFMTDBG`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:6460`

## `TEXUP`

- **`TEXUP_DBG`** — TEXUP_DBG=1: texture uploads per second. A cache that misses every draw re-uploads megabytes per frame, which is the kind of GPU work that trips a 2-second TDR while looking like nothing in a draw count. */
  `libs/video/rsx_d3d12_backend.c:1442`

## `TTY`

- **`TTY_BT`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:2281`  _(+1 more sites)_
- **`TTY_NO_DEDUPE`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:2246`

## `VBFULL`

- **`VBFULL`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:5595`  _(+1 more sites)_

## `VDATA`

- **`VDATA_DBG`** — _(no description in source)_
  `libs/video/rsx_commands.c:570`

## `VP`

- **`VP_ATTRS`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:5448`
- **`VP_ATTRS_DUMP`** — VP_ATTRS_DUMP=<attr>:<n>: read n entries of that attribute straight out of guest memory. Answers "did the guest write these values, or did our fetch mangle them" without inferring it from the rendered image. */
  `libs/video/rsx_d3d12_backend.c:5476`
- **`VP_ATTRS_FP`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:5453`
- **`VP_BYPASS`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:1791`
- **`VP_DUMP`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:1580`  _(+1 more sites)_
- **`VP_FIXPROJ`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:3183`
- **`VP_MVP`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:3116`
- **`VP_NOFIXPROJ`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:3182`
- **`VP_NOFREQ`** — _(no description in source)_
  `libs/video/rsx_vertex_fetch.c:55`
- **`VP_START_OFF`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:1739`
- **`VP_SUBMIT`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:4386`  _(+2 more sites)_
- **`VP_TESTTRI`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:4497`
- **`VP_W_ONE`** — VP_W_ONE=1: keep the guest transform but force the clip-space w to 1. Different from VP_BYPASS, which replaces the whole position: this tests only the hypothesis that w is wrong. This title's VP ends { float4 _v = (float4)((vp_c[467]).xxxx); o[0].w = _v.w; } with c467 = (-1, 1, 0, 0), so w = -1 and the clipper discards every vertex. Patched in place, space-padded so offsets stay valid. */
  `libs/video/rsx_d3d12_backend.c:1780`

## `VRAMSCAN`

- **`VRAMSCAN`** — VRAMSCAN=1: which 64 KB blocks of guest memory change between frames. "nothing writes the buffer we read" is only half an answer -- this says where the producer IS writing, so the two can be matched up. */
  `libs/video/rsx_d3d12_backend.c:4680`
- **`VRAMSCAN_BASE`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:4684`

## `VTX`

- **`VTX_DUMP`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:5572`  _(+2 more sites)_
- **`VTX_POS`** — _(no description in source)_
  `libs/video/rsx_d3d12_backend.c:5437`
- **`VTX_TYPEDBG`** — _(no description in source)_
  `libs/video/rsx_vertex_fetch.c:101`

## `WAITBT`

- **`WAITBT_EVERY`** — _(no description in source)_
  `runtime/syscalls/sys_event.c:332`

## `WATCHDOG`

- **`WATCHDOG_AT`** — _(no description in source)_
  `runtime/ppu/tests/boot_main.cpp:743`

## `YDKJ`

- **`YDKJ_ASSERTBT`** — YDKJ_ASSERTBT: the libspurs _cellSpursIsLaunchedFromTuner assertion (which aborts the SPURS task subsystem) prints through here. Dump the guest LR + back-chain to locate the asserting function so it can be suppressed. */
  `runtime/syscalls/lv2_register.c:123`
- **`YDKJ_CRIBT`** — CRI error back-chain (YDKJ_CRIBT=1): dump the guest LR chain when a CRI null-pointer / criFs error is printed, to locate the failing call. */
  `runtime/syscalls/lv2_register.c:100`
- **`YDKJ_CRI_CHAIN`** — YDKJ_CRI_R4: dump the CellSpursTaskset bitsets when the policy atomically touches my taskset (0x0F000000), to watch the task-activation state machine (why task0 isn't selected+first-run). running@0 ready@0x10 pending@0x20 enabled@0x30 signalled@0x40 waiting@0x50 (each 16B; word0 = MSB, task0=bit127). */
  `runtime/spu/spu_channels.c:319`  _(+2 more sites)_
- **`YDKJ_CRI_CREATE_OK`** — _(no description in source)_
  `runtime/ppu/ppu_hle.cpp:565`
- **`YDKJ_CRI_EVFLAG`** — _(no description in source)_
  `runtime/ppu/ppu_hle.cpp:330`
- **`YDKJ_CRI_EXIT_HALT`** — _(no description in source)_
  `runtime/spu/spu_channels.c:1298`  _(+1 more sites)_
- **`YDKJ_CRI_GATE1TRACE`** — YDKJ_CRI_GATE1TRACE=N: cri decode task busy-spins in the validator func_00026E80 (LS 0x26E80..0x26F14), a straight-line leaf that returns r3 = 0 / 0x8041090F / 0x80410909. Hand-decoding its selb/fsm/gb/ceqh lanes proved unreliable, so log the ACTUAL runtime result + inputs the first N times it returns. Fires on range-exit (pc left the fn), when r3 and the leaf's non-restored intermediates are still live. */
  `runtime/spu/spu_interp.c:351`
- **`YDKJ_CRI_RESUME`** — so it polls the (concurrently PPU-updated) eaContext, finds no work, yields and exits before the PPU marks work-ready. Approximate the resume by re-running the task (fresh context re-read from eaContext) in a bounded loop until it actually DECODES (a >256B video GET sets g_cri_video_dma) or we time out. This does NOT fake data: the task only decodes if the PPU genuinely populated real work meanwhile. */
  `runtime/spu/spu_workload.c:648`
- **`YDKJ_CRI_TASKSET`** — (image 22) calls the SPURS task-API via a jump table at LS 0x2700 and reads its task descriptor at LS 0x2FB0 — both POLICY-provided, not in the task ELF (so it branch-to-0's at func_00026DE0). Preload the policy module (libsre @0x30021480 -> LS 0xA00) and write the task descriptor {0xFFFFFFFF, 0x400, 0x2700, 0x3000} at 0x2FB0, then run the cri task, to see how much further it gets. Diagnostic only. */
  `runtime/spu/spu_workload.c:395`
- **`YDKJ_CRI_WAKE`** — YDKJ_CRI_WAKE probe: on cri task (image 22) completion, wake any PPU completion-waiter (the SPU->PPU cellSpursEventFlag completion isn't propagated yet). Tests whether the game then advances to draw content. */
  `runtime/spu/spu_workload.c:670`
- **`YDKJ_DMA_IMG`** — _(no description in source)_
  `runtime/spu/spu_dma.h:433`
- **`YDKJ_E60`** — _(no description in source)_
  `runtime/spu/spu_channels.c:107`
- **`YDKJ_EVFLAG_DIR`** — _(no description in source)_
  `runtime/ppu/ppu_hle.cpp:470`
- **`YDKJ_EVFLAG_INIT`** — nothing      -> 0x80410909 PERM          (+0xE == 0) DIR only     -> 0x80410911 NULL_POINTER  (a later field is 0) eager init   -> 0x8041090F STAT          (self-inflicted) So DIR-only is the furthest, and the real gap is that the title never fills +0xD/+0xE and the pointer field the claim dereferences. That code is title-side, not libsre. */
  `runtime/ppu/ppu_hle.cpp:498`
- **`YDKJ_EVT_LENIENT`** — idempotent teardown
  `runtime/syscalls/sys_event.c:216`  _(+6 more sites)_
- **`YDKJ_FAKECOMPLETE`** — YDKJ_FAKECOMPLETE (diagnostic): the SPURS task-completion events the SPU would post to q=2/q=3 never arrive (SPU video task not running), so the main thread times out after 30s and the title tears down. Synthesize a completion so the wait returns -> see whether the game then advances to asset load + menu draw (content). Returns immediately with a zero event. */
  `runtime/syscalls/sys_event.c:412`
- **`YDKJ_FORCEATTR`** — YDKJ_FORCEATTR (test): the create-task nid 0x87630976 (libsre 0x300158C4) STATs because the task-attr struct(r3)+0xC != 0xFF -- the game runs create before the attr-init (0x22AAB31D) that stamps it (a game-side vtable ordering mis-lift). Force +0xC=0xFF here to test whether the task then creates + the workload attaches + the cri decode runs (papering the ordering to probe the NEXT gate). */
  `runtime/ppu/ppu_hle.cpp:407`
- **`YDKJ_FORCE_HLE`** — _(no description in source)_
  `runtime/ppu/ppu_hle.cpp:309`
- **`YDKJ_G1_CTX`** — _(no description in source)_
  `runtime/spu/spu_workload.c:480`
- **`YDKJ_G1_LSPAT`** — _(no description in source)_
  `runtime/spu/spu_workload.c:478`
- **`YDKJ_GUESTINIT`** — YDKJ_GUESTINIT: the create-task (0x87630976) runs BEFORE the game's own init of its task descriptor (r3), so descriptor+0xC != 0xFF -> STAT. Eagerly invoke the REAL init (0x22AAB31D) on the descriptor first via the guest-caller (ppu_guest_call runs it in a scratch ctx, so the create's args survive). This does the FULL init incl. the task-slot alloc syscall -- unlike the raw +0xC force (FORCEATTR) that hung -- then create proceeds against a valid descriptor. */
  `runtime/ppu/ppu_hle.cpp:418`
- **`YDKJ_GUESTINIT_CRI`** — skip audio
  `runtime/ppu/ppu_hle.cpp:424`
- **`YDKJ_HANDLERFIX`** — _(no description in source)_
  `libs/video/cellGcmSys.c:178`
- **`YDKJ_HLE_DRAW`** — YDKJ_HLE_DRAW (diagnostic): also unblock the AsyncLoad q=1 wait (timeout=0, blocks forever) so the loader thread proceeds. Tests whether the game's render/draw code is reachable once the completion waits are satisfied. */
  `runtime/syscalls/sys_event.c:422`
- **`YDKJ_INJECT_PAD`** — _(no description in source)_
  `libs/input/cellPad.c:529`
- **`YDKJ_INJECT_Q3`** — YDKJ_INJECT_Q3: the main thread polls q3 (load/state-complete) each frame but it's never posted (producer signals a condvar, never enqueues). Inject a q3 event periodically to test whether delivering the completion advances the game's state machine to instantiate + display the (force-parsed) menu movie. */
  `runtime/ppu/tests/boot_main.cpp:420`  _(+1 more sites)_
- **`YDKJ_KEEPGROUP`** — EXPERIMENT (YDKJ_KEEPGROUP): libsre rolls back the SPURS kernel group during cellSpursInitialize (the handler asserts the SPU side is dead). Skip the destroy so the group + threads survive, to see whether libsre then proceeds (group_start) or just re-asserts. Logs the caller for diagnosis. */
  `runtime/syscalls/lv2_register.c:1093`
- **`YDKJ_MEMFIX`** — Override the mis-lifted optimized memmove with a correct native one. */
  `runtime/ppu/ppu_loader.cpp:1433`
- **`YDKJ_MINIMAL_CTX`** — _(no description in source)_
  `runtime/spu/spu_workload.c:443`
- **`YDKJ_NETOFFLINE`** — YDKJ_NETOFFLINE: unresolved network NIDs (sys_net/cellHttp/cellSysutil) default r3=0 (fake success) -> the game stores a NULL handle as an object and later virtual-calls it -> the 0xC708C708 null-vtable crash. Return a negative error instead so the game's online-content check takes its graceful OFFLINE-FAILURE path and proceeds with the local Persistent.zip. */
  `runtime/ppu/ppu_hle.cpp:660`
- **`YDKJ_NOEXIT`** — YDKJ_NOEXIT (diagnostic): the title self-exits after ~2 frames on a teardown assert triggered by incomplete SPU/SPURS state (a timing race). Ignoring the exit on the calling thread lets the render loop keep running so we can see whether sustained execution produces on-screen content. */
  `libs/system/sysPrxForUser.c:172`
- **`YDKJ_NOHDLR`** — Diagnostic (YDKJ_NOHDLR): suppress libsre's SPURS handler threads (entry in the libsre image range) -- they assert that the SPU side isn't operational and crash. Skipping them lets the main thread (already past cellSpursInitialize) keep running, to see how far it gets. The thread is "created" (tid returned) but never spawned. */
  `runtime/syscalls/sys_ppu_thread.c:417`
- **`YDKJ_NO_CRI_CTX`** — task ELF directly, so LS 0x2700 is empty and the task branch-to-0's: cri func_00026DE0 reads syscallAddr@0x27C4 (=0) and branches there. Plant a minimal context so the task-API call lands on the HLE syscall trampoline (LS 0xA70, intercepted in spu_channels.c) + the task descriptor @0x2FB0. Adopted from JonathanDC64/ps3recomp (aaea4158). Gate: default on for image 22 unless YDKJ_NO_CRI_CTX. */
  `runtime/spu/spu_workload.c:439`
- **`YDKJ_NO_CRI_R4`** — YDKJ_CRI_R4: the taskset policy entry (LS 0xA00, image 23) writes r4 into SpursTasksetContext.taskset @LS 0x27B8 (per RPCS3 cellSpursSpu.cpp). Our kernel->policy handoff doesn't convey the taskset EA, so the policy DMAs the taskset from garbage -> waiting!=0 -> wrong resume path -> savedContextLr=0. Inject r4 = taskset EA (0x0F000000) at the policy entry dispatch (this is the exact point before the entry reads r4, after the kernel's arg setup). */
  `runtime/spu/spu_channels.c:1602`  _(+1 more sites)_
- **`YDKJ_NO_PORTROUTE`** — _(no description in source)_
  `runtime/syscalls/sys_event.c:941`
- **`YDKJ_NO_REENTRY_ISO`** — results go via r3 / guest pointers, so isolating the frame is safe. */ Default ON: without isolation the libsre fn runs at the caller's r1 and its prologue `std r2,40(r1)` clobbers the caller's reserved TOC slot at 0x28(r1) -> caller reloads r2=0 -> null TOC -> func_002B03AC spin -> no draws. Isolating r1 to a fresh slice keeps the caller frame intact. Opt out with YDKJ_NO_REENTRY_ISO=1. */
  `runtime/ppu/ppu_hle.cpp:543`
- **`YDKJ_PACETRACE`** — _(no description in source)_
  `runtime/ppu/tests/boot_main.cpp:426`
- **`YDKJ_PAD_BTN`** — _(no description in source)_
  `libs/input/cellPad.c:530`
- **`YDKJ_PAD_DELAY`** — _(no description in source)_
  `libs/input/cellPad.c:531`
- **`YDKJ_POLTRACE`** — _(no description in source)_
  `runtime/spu/spu_channels.c:1646`
- **`YDKJ_Q23STACK`** — Diagnostic: main-thread per-frame poll on q=2/q=3 (timeout ~30us) never advances to content. Dump its guest call chain to identify the frame-loop function + the value it waits on. Fires a few times. YDKJ_Q23STACK. */
  `runtime/syscalls/sys_event.c:435`
- **`YDKJ_RDY_D2`** — Match the observed real port_send(port=4) shape: data1=0, data2=1, data3=0. source is the port name; use a SPURS-ish tag. Env YDKJ_RDY_D2 overrides data2 for quick format experiments. */
  `runtime/syscalls/sys_event.c:396`
- **`YDKJ_RWATCH`** — _(no description in source)_
  `runtime/ppu/ppu_loader.cpp:1007`
- **`YDKJ_SAVEDATA`** — protocol so it can be implemented. */ YDKJ_SAVEDATA: minimal cellSaveDataAutoLoad2 (NID 0xCDC6AEFD). The game auto-loads its "BLUS30569-AUTO-" save; unimplemented = null completion = state never transitions. Implement the no-save/new-user path: invoke the game's statCallback (r8 OPD) with a CellSaveDataStatGet{isNewData=1}, honor its result, return CELL_SAVEDATA_RET_OK(0). Legit HLE (real first-run behavior). */
  `runtime/ppu/ppu_hle.cpp:629`
- **`YDKJ_SPUREADY`** — EXPERIMENT (YDKJ_SPUREADY): the lifted libsre cellSpursInitialize busy-polls the SPU-thread descriptor field at img_ea+0x38 (e.g. 0x101671A8) for a non-zero "thread created/ready" status BEFORE it populates the SPURS instance / starts the group — but nothing in our HLE ever writes it (real lv2 does). Write the tid there to clear the poll so libsre can proceed past init. Diagnostic; value may need tuning. */
  `runtime/syscalls/lv2_register.c:597`
- **`YDKJ_SPURSKERNEL`** — Empty image (entry=0) OR the real SPURS kernel-A entry (0x818, now that _sys_spu_image_import parses the kernel ELF) on a cellSpurs SPU thread -> route to the HLE SPURS kernel (YDKJ_SPURSKERNEL) so group_start runs a live SPU instead of an instant no-op. */
  `runtime/syscalls/lv2_register.c:566`
- **`YDKJ_SPURS_READY`** — PPU side. cellSpursInitialize blocks forever (timeout=0) on the SPURS event queues (q=1/q=4) awaiting the kernel/policy "ready" event, while the policy (now running real code) waits for a workload the PPU only adds AFTER init returns. Synthesize a ready event so init returns -> see whether the PPU then reaches CreateTaskset and the running policy dispatches the cri task. Returns a zero-ish SPU-thread-group event; refine the format if init rejects it. */
  `runtime/syscalls/sys_event.c:386`
- **`YDKJ_SPUTASK`** — SPURS bring-up: when a PPU thread is about to block forever for SPU completion (libsre created the taskset correctly but never started its SPU kernel), drive the title's REAL lifted SPU task runtime (image 22 @guest 0x004F5F80) against the recorded SPURS context. It claims the ready task from libsre's taskset and runs the task body -- real recompiled SPU code, not a synthesized completion. Gated by YDKJ_SPUTASK; fire once. */
  `runtime/syscalls/sys_event.c:365`
- **`YDKJ_SPU_LSDUMP`** — State-diff oracle: dump full LS + all GPRs at the branch-to-0 so it can be compared byte-for-byte against the RPCS3 savestate LS of the same (cri_mpv) task. Path from YDKJ_SPU_LSDUMP, else ./recomp_spu_ls.bin. */
  `runtime/spu/spu_channels.c:2093`
- **`YDKJ_SPU_TRACE`** — YDKJ_SPU_TRACE=N: log the last N PCs into a ring buffer and dump them when the interp halts -- shows the path to a branch-to-0 (the cri task/policy wall). */
  `runtime/spu/spu_interp.c:344`
- **`YDKJ_STOP_HALT`** — _(no description in source)_
  `runtime/spu/spu_channels.c:137`
- **`YDKJ_SYNCLOG`** — _(no description in source)_
  `runtime/syscalls/sys_cond.c:77`  _(+2 more sites)_
- **`YDKJ_SYSCALL_ARG`** — The task-API argument is passed in LOCAL STORE at 0x2FD0, not in r4: the caller does shufb(arg,...) -> stqd 0x2FD0 and only then sets r3 = number (see func_00026DE0 / func_000272AC in image 22). Logging r4 shows caller leftovers and hides what the task actually asked for. */
  `runtime/spu/spu_channels.c:1275`
- **`YDKJ_TUNERFIX`** — YDKJ_TUNERFIX (at the top so it wins over any registered handler): the profiler-presence query sysPrxForUser 0xE0998DBF, called by libsre _cellSpursIsLaunchedFromTuner (0x3000D318), must return 0x8001112E ("profiler not loaded") on a normal run. Anything else trips the usertrace.c:123 assert AND makes the fn report "launched from tuner", so the caller runs tuner/trace setup that fails with CELL_SPURS_CORE_ERROR_STAT -> the SPURS task workload never attaches. */
  `runtime/ppu/ppu_hle.cpp:223`  _(+1 more sites)_

